# .NET control plane

This directory will contain the Avalonia dashboard, generated ChoreoOS client,
Temporal workflow definitions, separately executable Temporal Worker, and .NET
tests. It never owns authoritative choreography or consensus state.
